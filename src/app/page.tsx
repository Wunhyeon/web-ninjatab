import { createClient } from "@/utils/supabase/server";

export default async function Home() {
  return (
    <main className="relative flex flex-col text-slate-700  bg-white dark:bg-slate-900"></main>
  );
}
